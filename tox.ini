[tox]
envlist = py35

[testenv]
commands = py.test --capture=no --cov=sublimepost --cov-report=html tests
deps = pytest
       requests
       aiohttp
       aiohttp-jinja2
       jinja2
       path.py
       pytest-cov
